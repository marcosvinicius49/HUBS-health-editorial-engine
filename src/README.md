# src

Runtime do Astro SSG. Não substitui a árvore editorial da raiz.

- `content.config.ts` — collections apontando para `content/`, `data/` e `videos/`
- `pages/` — origem estática do motor (`/` e `404`); templates de revista não entram aqui neste estágio
