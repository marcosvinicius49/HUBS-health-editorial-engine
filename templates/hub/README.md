# templates/hub

Esta pasta **não** define um layout separado.

A página de cluster usa o contrato em [`../category/`](../category/README.md). O grafo de pilares continua em `hubs/` na raiz do repositório.

Não implementar HTML aqui.
