# docs

Documentação do motor editorial. Não é conteúdo publicado.

Subpastas previstas:

- `architecture/` — pastas, fluxos e contratos entre camadas
- `editorial/` — fluxo de publicação, hubs e localização
- `seo/` — regras que o motor editorial deve seguir (não é artefato publicado)

Arquivos em `docs/seo/`:

- `seo-architecture.md` — camadas, domínio, idioma, canônico, schema, sitemap
- `hub-spoke-model.md` — pilares, spokes e ligação interna
- `content-rules.md` — qualidade, YMYL, draft, tradução
- `video-seo.md` — URL de vídeo, `VideoObject`, transcrição
